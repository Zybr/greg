extends ../../core/index
block head
  script(src="/public/index.js")

block body
  form(action=socket / data)
    div
        input(type=text name='search' value='search')
    div
        button(class='search') search
        button(class='stop') stop
